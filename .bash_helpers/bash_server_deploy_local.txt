# Example of ssr_deploy_local bash helper.

alias ssr_deploy_local="bash .scripts/deploy-local.sh -profile='ssr'"

### Prod specific secret
if [ -f ~/.bash_prod_local ]; then
    . ~/.bash_prod_local
fi
